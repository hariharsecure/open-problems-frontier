# A297484 — OEIS submission package (M35)

**Sequence:** A297484 — Number of maximal matchings in the Johnson graph J(n,2) (triangular graph, line graph of K_n)
**Keyword:** `more` (b-file was open-ended)
**New certified terms:** a(8), a(9)  (2 terms)
**Last existing b-file term:** a(7) = 24070032

---

## 1. b-file extension lines (append after the current endpoint)

```
8 59779624320
9 650627568460800
```

(Indices continue directly from the existing b-file endpoint. Certified dual-lineage terms only.)

## 2. Ready-to-paste comment / annotation

a(8)-a(9) computed by an exact combinatorial reduction a(n) = Sum_k M(n,k)*Phi(K_n minus a k-matching), where M(n,k) counts k-matchings of K_n and Phi(H) counts P3-decompositions of H (evaluated exactly as a Gaussian moment by big-integer variable elimination). The program reproduces every previously published b-file term (a(2)..a(7)) exactly, and each new term was independently verified by two distinct implementations (a second, independently-authored program using a different formulation) that agree digit-for-digit.

## 3. Program reference

- Reproducible program: `research/M35/solve.py`
- A second, independent program (different formulation/lineage) also reproduces all prior terms and every new term above, digit-for-digit; see `research/M35/` (e.g. the Codex lineage / alternate solver in that directory).
- Full method, gate table, and dual-lineage certification: `research/M35/RESULT.md`.
