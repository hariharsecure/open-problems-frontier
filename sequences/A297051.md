# A297051 — OEIS submission package, wave 2 (M37 / SCOUT_F)

**Sequence:** A297051 — Number of edge covers in the n-Fibonacci cube graph
**Keyword:** `nonn,more`
**New certified term:** a(9) (1 term)
**Last existing published term:** a(8) = 117497717101650539860985028475855893600

---

## 1. b-file extension line (append after the current endpoint)

```
9 3348177365992530459596530969749430504528623432257728266402865066788765
```

## 2. Ready-to-paste comment / annotation

a(9) computed by an exact bounded-frontier dynamic program over the 9-Fibonacci-cube
graph Gamma_9 (89 vertices, width=22): each active vertex carries an "already covered"
bit (subsumed by >=1 selected incident edge so far), with a vertex forgotten only if
covered (an edge cover need not be minimal -- any covering edge subset counts).
Reproduces a(1..8) exactly before trusting a(9) (feasibility-first). Implemented twice
independently in-house (a frozenset-keyed version and a bitmask/free-list version; both
agree digit-for-digit). A completely independent third program (own graph constructor,
own vertex order (width=19, via a tensor/inclusion-exclusion-style frontier formulation),
written by Codex/GPT-5 with no access to the primary implementation) reproduces the full
published gate and returns the SAME value, digit-for-digit.

## 3. Program reference

- Reproducible program (Lineage 1, mine): `programs/ (this repo)`
  (bitmask engine); frozenset cross-check run inline (see `programs/ (this repo)`).
- Independent cross-check (Lineage 2, Codex): `programs/ (this repo)`
  (Task C); raw output `programs/ (this repo)` (`GAMMA9_EDGE_COVERS`).
- Full method, gate table, and dual-lineage certification ledger:
  `programs/ (this repo)`.
