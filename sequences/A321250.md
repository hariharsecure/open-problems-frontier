# A321250 — OEIS submission package (M24)

**Sequence:** A321250 — Number of maximal independent sets in the n X n torus grid graph C_n [] C_n
**Keyword:** `more` (b-file was open-ended)
**New certified terms:** a(12), a(13)  (2 terms)
**Last existing b-file term:** a(11) = 682849731586

---

## 1. b-file extension lines (append after the current endpoint)

```
12 122265164050812
13 34294930940467990
```

(Indices continue directly from the existing b-file endpoint. Certified dual-lineage terms only.)

## 2. Ready-to-paste comment / annotation

a(12)-a(13) computed by an exact deterministic column transfer-matrix over the big-integer state counts (per-column occupancy + pending-domination profile, horizontal torus wrap closed by a column-0 anchor with a residual seam mask, vertical wrap by cyclic column adjacency). The program reproduces every previously published b-file term (a(1)..a(11)) exactly, and each new term was independently verified by two distinct implementations (a second, independently-authored program using a different formulation) that agree digit-for-digit.

## 3. Program reference

- Reproducible program: `programs/a321250.py.txt`
- A second, independent program (different formulation/lineage) also reproduces all prior terms and every new term above, digit-for-digit; see `programs/a321250.py.txt` (e.g. the Codex lineage / alternate solver in that directory).
- Full method, gate table, and dual-lineage certification: `programs/a321250.py.txt`.
