# A297486 — OEIS submission package (M29)

**Sequence:** A297486 — Number of maximal matchings in the n X n torus grid graph
**Keyword:** `more` (b-file was open-ended)
**New certified terms:** a(10)  (1 term)
**Last existing b-file term:** a(9) = 124523589644006520

---

## 1. b-file extension lines (append after the current endpoint)

```
10 1281152829402802844888
```

(Indices continue directly from the existing b-file endpoint. Certified dual-lineage terms only.)

## 2. Ready-to-paste comment / annotation

a(10) computed by an exact transfer-matrix with trace(T^n) for the horizontal torus wrap (per-row 3-state boundary profile, vertical wrap closed inside the column transition, sped up by a dihedral row-orbit reduction). The program reproduces every previously published b-file term (a(3)..a(9)) exactly, and each new term was independently verified by two distinct implementations (a second, independently-authored program using a different formulation) that agree digit-for-digit.

## 3. Program reference

- Reproducible program: `research/M29/solve.py`
- A second, independent program (different formulation/lineage) also reproduces all prior terms and every new term above, digit-for-digit; see `research/M29/` (e.g. the Codex lineage / alternate solver in that directory).
- Full method, gate table, and dual-lineage certification: `research/M29/RESULT.md`.
