# A288027 — OEIS submission package (M30)

**Sequence:** A288027 — Number of minimal edge covers in the n X n grid graph P_n X P_n
**Keyword:** `more` (b-file was open-ended)
**New certified terms:** a(10), a(11)  (2 terms)
**Last existing b-file term:** a(9) = 749707067231291403016

---

## 1. b-file extension lines (append after the current endpoint)

```
10 145937044570339679147782926
11 110740872419155899579045292640846
```

(Indices continue directly from the existing b-file endpoint. Certified dual-lineage terms only.)

## 2. Ready-to-paste comment / annotation

a(10)-a(11) computed by an exact column transfer-matrix over big integers (per-row incoming-edge constraint class, counting subgraphs in which every component is a star, i.e. min degree >= 1 and every edge has a degree-1 endpoint). The program reproduces every previously published b-file term (a(1)..a(9)) exactly, and each new term was independently verified by two distinct implementations (a second, independently-authored program using a different formulation) that agree digit-for-digit.

## 3. Program reference

- Reproducible program: `programs/a288027.py.txt`
- A second, independent program (different formulation/lineage) also reproduces all prior terms and every new term above, digit-for-digit; see `programs/a288027.py.txt` (e.g. the Codex lineage / alternate solver in that directory).
- Full method, gate table, and dual-lineage certification: `programs/a288027.py.txt`.
