# A370573 — OEIS submission package (M39 / SCOUT_C)

**Sequence:** A370573 — Number of connected dominating sets in the n-Lucas cube graph Λ_n
**Keyword:** `nonn,more`
**New certified terms:** a(8)  (1 term)
**Last existing b-file term:** a(7) = 57241196

---

## 1. b-file extension lines (append after the current endpoint)

```
8 12009837417244
```

## 2. Ready-to-paste comment / annotation

a(8) computed by an exact frontier/boundary DP that tracks, in addition to the domination status of
out-vertices, the partition of in-set frontier vertices into connected components (a component may be
"sealed" only when its last frontier member is forgotten, and at most one component may ever seal —
enforcing that the induced subgraph on S is connected). This was cross-checked against an independent
connectivity-aware exact counter (a separate implementation, Codex lineage). Both engines reproduce
all published terms a(1)..a(7) exactly and agree digit-for-digit on a(8); both match brute force
(dominating + induced-connected) for n<=6.

## 3. Program reference

- Reproducible program (lineage A, component-partition frontier DP): `programs/a370573.py.txt`
- Independent program (lineage B, Codex connectivity-aware counter):
  `programs/a370573.py.txt`
- Full method, gate table, and dual-lineage certification: `programs/a370573.py.txt`.
