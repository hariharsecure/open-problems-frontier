# A291935 — OEIS submission package (M37 / SCOUT_F)

**Sequence:** A291935 — Number of matchings in the n-Fibonacci cube graph
**Keyword:** `nonn,more` (author computation stalled at a(8))
**New certified term:** a(9) (1 term)
**Last existing published term:** a(8) = 66878421392025692

---

## 1. b-file extension line (append after the current endpoint)

```
9 36196981583362467571542074984
```

(Index continues directly from the existing b-file endpoint. Dual-lineage certified.)

## 2. Ready-to-paste comment / annotation

a(9) computed by an exact bounded-frontier ("path-decomposition") dynamic program over
the 9-Fibonacci-cube graph Gamma_9 (89 vertices): vertices are processed in an
independently-chosen linear order (best of natural/reverse-Cuthill-McKee/BFS orderings
by measured width, width=22 for Gamma_9); a vertex is "active" from its introduction
until the position of its last-processed neighbour, tracking per-vertex matched/unmatched
status in a bitmask state (dict: bitmask -> exact big-integer count). The program
reproduces every previously published b-file term (a(1)..a(8)) exactly before computing
a(9) (feasibility-first gate), and was implemented TWICE independently in-house
(a frozenset-keyed version and a bitmask/free-list version; both agree digit-for-digit on
every value, including a(9)). A completely independent third program (different graph
constructor, different elimination-ordering search converging on a different vertex order
of width 19, different DP formulation, written by Codex/GPT-5 with no access to the
primary implementation) reproduces the full published gate and returns the SAME value for
a(9), digit-for-digit.

## 3. Program reference

- Reproducible program (Lineage 1, mine): `programs/ (this repo)`
  (bitmask engine) and `programs/ (this repo)` (frozenset engine, agrees
  exactly); graph construction in `programs/ (this repo)`.
- Independent cross-check (Lineage 2, Codex): `programs/ (this repo)`
  (own graph construction + own elimination-order search + own frontier DP);
  raw output in `programs/ (this repo)`.
- Full method, gate table, and dual-lineage certification ledger:
  `programs/ (this repo)`.
