# A287595 — OEIS submission package (M25)

**Sequence:** A287595 — Number of maximal matchings in the n X n grid graph
**Keyword:** `more` (b-file was open-ended)
**New certified terms:** a(13)  (1 term)
**Last existing b-file term:** a(12) = 17941038966060235808302667164

---

## 1. b-file extension lines (append after the current endpoint)

```
13 2280413203760054215839279592187056
```

(Indices continue directly from the existing b-file endpoint. Certified dual-lineage terms only.)

## 2. Ready-to-paste comment / annotation

a(13) computed by an exact column transfer-matrix over big integers (per-row 3-state boundary profile: matched / outgoing-horizontal-edge / pending-unmatched, with maximality enforced as an independent unmatched set). The program reproduces every previously published b-file term (a(0)..a(12)) exactly, and each new term was independently verified by two distinct implementations (a second, independently-authored program using a different formulation) that agree digit-for-digit.

## 3. Program reference

- Reproducible program: `programs/a287595.py.txt`
- A second, independent program (different formulation/lineage) also reproduces all prior terms and every new term above, digit-for-digit; see `programs/a287595.py.txt` (e.g. the Codex lineage / alternate solver in that directory).
- Full method, gate table, and dual-lineage certification: `programs/a287595.py.txt`.
