# A298115 — OEIS submission package (M39 / SCOUT_C)

**Sequence:** A298115 — Number of connected dominating sets in the n-Fibonacci cube graph Γ_n
**Keyword:** `nonn,more`
**New certified terms:** a(8)  (1 term)
**Last existing b-file term:** a(7) = 2135825375

---

## 1. b-file extension lines (append after the current endpoint)

```
8 3708334309546496
```

## 2. Ready-to-paste comment / annotation

a(8) computed by an exact frontier/boundary DP that tracks the domination status of out-vertices
together with the partition of in-set frontier vertices into connected components (a component may
"seal" only when its last frontier member is forgotten, and at most one component may ever seal —
enforcing that the induced subgraph on S is connected). A spectral (Fiedler-vector) vertex ordering
keeps the boundary width small (14 for Γ_8), which collapses the partition state space (peak 1.37M
states, 0.35 GB). Cross-checked against an independently implemented connectivity-aware exact counter
(Codex lineage). Both engines reproduce all published terms a(1)..a(7) exactly and agree
digit-for-digit on a(8); both match brute force (dominating + induced-connected) for n<=6.

## 3. Program reference

- Reproducible program (lineage A, component-partition frontier DP, Fiedler order):
  `programs/ (this repo)` + `programs/ (this repo)`
- Independent program (lineage B, Codex connectivity-aware counter):
  `programs/ (this repo)`
- Full method, gate table, and dual-lineage certification: `programs/ (this repo)`.
