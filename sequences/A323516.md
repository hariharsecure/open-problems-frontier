# A323516 — OEIS submission package (SCOUT_D)

**Sequence:** A323516 — Number of minimum total dominating sets in the n-Sierpinski tetrahedron graph
**Keyword:** nonn,more,bref (3 published terms)
**New certified terms:** a(4), a(5)  (2 terms)
**Last existing term:** a(3) = 2430

---

## 1. b-file extension lines (append after the current endpoint a(3))

```
4 3386054321784720
5 9062470560451391467638709000896766497436441452208709815244567040
```

(Offset 1. γ_t(ST_n) = 2, 3, 8, 32, 128 for n=1..5. Certified dual-lineage terms only.
a(4): full-poly renormalization ⟂ Codex transfer/C++ brute + explicit ST_3 enumeration
(all C(34,8) subsets). a(5): bottom-windowed renormalization ⟂ Codex C++/GMP transfer,
window-independent (T=8 ≡ T=24).)

## 2. Ready-to-paste comment / annotation

a(5) computed by an exact self-similar renormalization over the 4-corner boundary
state of the Sierpinski tetrahedron graph ST_n (ST_1 = K_4; ST_n = four copies of
ST_(n-1) with corner j of copy i identified to corner i of copy j — 6 shared corners,
4 outer corners; |V(ST_n)| = 4|V(ST_(n-1))| - 6 = 4,10,34,130,514,2050,8194; every edge
lies inside one copy, #edges = 6*4^(n-1)). Per corner the transfer records corner state (s,d): s = corner in S, d = corner already has a neighbour in S inside the gadget; shared/outer corners must be totally dominated (d=1); a(n)=min-degree coefficient of the x^|S| generating polynomial via an exact bottom-window (T=8, confirmed window-independent at T=24);
the 6 shared corners are closed at each level and the 4 outer corners at the top, and
a(n) is the minimum-degree coefficient of the exact generating polynomial. The method reproduces every
published term exactly, and the new term was verified by two independently-authored
programs (a Python renormalization and an independent C++/GMP transfer) that agree
digit-for-digit, both also passing an explicit-graph brute-force check on ST_1 and ST_2.

## 3. Program reference

- Reproducible program (renormalization, all four cluster sequences):
  `research/SCOUT_D/cluster.py`  (value = `tdom_engine(n,WinBot) min-degree`).
- Independent lineage (different formulation, digit-for-digit agreement): Codex C++/GMP
  transfer + explicit brute, `research/SCOUT_D/codex_log2.txt` (asserts vs ST_1/ST_2).
- Independent explicit-graph anchor (ST_1, ST_2): `research/SCOUT_D/brute_cluster.py`.
- Full method + dual-lineage certification + runtimes: `research/SCOUT_D/RESULT_cluster.md`.
